name: parity

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  azazel:
    name: azazel builds a zmath consumer
    runs-on: ubuntu-latest
    permissions:
      contents: write   # upload build artifacts to this repo's 'cache' release
    steps:
      - uses: actions/checkout@v4
      - name: Cache Zig global cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/zig
          key: zig-cache-${{ runner.os }}-${{ github.job }}-${{ hashFiles('**/build.zig.zon') }}
          restore-keys: |
            zig-cache-${{ runner.os }}-${{ github.job }}-
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - name: Install CUE
        run: |
          CUE_VERSION=v0.11.1
          curl -sL "https://github.com/cue-lang/cue/releases/download/${CUE_VERSION}/cue_${CUE_VERSION}_linux_amd64.tar.gz" -o /tmp/cue.tgz
          sudo tar -xzf /tmp/cue.tgz -C /usr/local/bin cue
      - name: Build zmath consumer via azazel (CUE model)
        working-directory: azazel
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # free model-keyed shared cache: HIT downloads zig-out and skips the build
          time sh cache_build.sh
          ./zig-out/bin/zmath_probe
  zaza:
    name: zaza builds a zmath consumer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cache Zig global cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/zig
          key: zig-cache-${{ runner.os }}-${{ github.job }}-${{ hashFiles('**/build.zig.zon') }}
          restore-keys: |
            zig-cache-${{ runner.os }}-${{ github.job }}-
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - name: Build + run the zmath consumer via zaza
        working-directory: zaza
        run: time zig build run
